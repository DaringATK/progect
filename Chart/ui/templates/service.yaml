---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}
  labels:
    app: reddit
    component: ui
    release: {{ .Release.Name }}
spec:
  type: NodePort
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000    
  selector:
    app: reddit
    component: ui
    release: {{ .Release.Name }}
